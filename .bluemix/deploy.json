{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "messages": {
        "$i18n": "locales.yml"
    },
    "title": {
        "$ref": "#/messages/deploy.title"
    },
    "description": {
        "$ref": "#/messages/deploy.description"
    },
    "longDescription": {
        "$ref": "#/messages/deploy.longDescription"
    },
    "type": "object",
    "properties": {
        "org-admin-identity": {
            "description": {
                "$ref": "#/messages/deploy.organizationAdminIdentity"
            },
            "type": "string",
            "pattern": "^.{1,50}$"
        },
        "org-connection-profile": {
            "description": {
                "$ref": "#/messages/deploy.organizationConnectionProfile"
            },
            "type": "string",
            "pattern": "^.{1,50}$"
        }
    },
    "required": [
        "org-admin-identity",
        "org-connection-profile"
    ],
    "form": [
        {
            "type": "table",
            "columnCount": 2,
            "widths": [ "50%", "50%" ],
            "items": [
                {
                    "type": "label",
                    "title": {
                        "$ref": "#/messages/organizationAdminIdentity"
                    }
                },
                {
                    "type": "string",
                    "key": "org-admin-identity"
                },

                {
                    "type": "label",
                    "title": {
                        "$ref": "#/messages/organizationConnectionProfile"
                    }
                },
                {
                    "type": "string",
                    "key": "org-connection-profile"
                }
            ]
        }
    ]
}