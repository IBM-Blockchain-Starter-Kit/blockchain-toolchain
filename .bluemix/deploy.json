{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "messages": {
        "$i18n": "locales.yml"
    },
    "title": {
        "$ref": "#/messages/deploy.title"
    },
    "description": {
        "$ref": "#/messages/deploy.description"
    },
    "longDescription": {
        "$ref": "#/messages/deploy.longDescription"
    },
    "type": "object",
    "properties": {
        "org-admin-identity": {
            "title": "Admin Identity (JSON formatted)",
            "description": {
                "$ref": "#/messages/deploy.organizationAdminIdentity"
            },
            "type": "string",
            "default": "{ ... }"
        },
        "org-connection-profile": {
            "title": "Channel Profile (JSON formatted)",
            "description": {
                "$ref": "#/messages/deploy.organizationConnectionProfile"
            },
            "type": "string",
            "default": "{ ... }"
        }
    },
    "required": [
        "org-admin-identity",
        "org-connection-profile"
    ],
    "form": [
        {
            "type": "table",
            "columnCount": 2,
            "widths": [ "20%", "80%" ],
            "items": [
                "org-admin-identity",
                "org-connection-profile"
            ]
        }
    ]
}